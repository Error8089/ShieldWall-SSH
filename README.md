# 🛡️ ShieldWall-SSH: Защита от Брутфорса

ShieldWall-SSH — это легковесная система предотвращения вторжений (IPS), написанная на Python специально для **Fedora** и систем, использующих `firewalld`.

## 📖 Описание
Скрипт в реальном времени анализирует системные логи аутентификации (`/var/log/secure`). Если фиксируется серия неудачных попыток входа по SSH с одного IP-адреса, ShieldWall автоматически блокирует этот адрес через системный фаервол.

## ✨ Основные возможности
- **Мониторинг в реальном времени**: Мгновенная реакция на попытки подбора пароля.
- **Интеграция с Firewalld**: Использует современные `rich-rules` для блокировки, не ломая остальные настройки сети.
- **Автоматизация**: Работает в фоне и защищает сервер без участия администратора.
- **Безопасность**: Позволяет гибко настраивать порог срабатывания (количество попыток до бана).

## 🛠️ Как это работает
Программа работает по следующему алгоритму:
1. Читает поток обновлений файла `/var/log/secure`.
2. Использует регулярные выражения для поиска строк `Failed password`.
3. Считает количество ошибок для каждого уникального IP.
4. При достижении лимита (например, 5 попыток) выполняет команду:
   `firewall-cmd --add-rich-rule='rule family="ipv4" source address="IP" reject'`

## 🚀 Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Error8089/ShieldWall-SSH.git
   cd ShieldWall-SSH

    Запустите скрипт с правами суперпользователя (необходимо для работы с логами и фаерволом):

    sudo python3 shieldwall.py
